package week05;
public class CompareData {
    public static void main(String[] args) {
        //การเปรียนเทียบ ผลลัพธ์จะได้ True /False
        int a = 10; int b = 5;
        System.out.println(a+ "มีค่าเท่ากับ "+b+"หรือไม่ ? " + (a==b)); 
        System.out.println(a+ "มีค่าเท่ากับ "+b+"หรือไม่ ? " + (a!=b)); 
        System.out.println(a+ "มีค่าเท่ากับ "+b+"หรือไม่ ? " + (a==b)); 
        
        //boolean c = a==b 
        //System.out.println(c);;
    }   
    
}
